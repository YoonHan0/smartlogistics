<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="business">
	<select id="findAll" resultType="businessvo">
		<![CDATA[
			select id, name, phone, ip, date_format(input_date, '%Y-%m-%d') as inputDate, user_no
			from business
			ORDER BY id ASC
		]]>
	</select>
	
	<select id="findAllByKeyword" parameterType="businessvo" resultType="businessvo">
		<![CDATA[
			SELECT id, name, phone
			FROM business 
			WHERE id LIKE '%${id}%' AND name LIKE '%${name}%' AND phone LIKE '%${phone}%'
		]]>
	</select>
	
	<insert id="insert" parameterType="businessvo">
		<![CDATA[
		insert
		  into business
		values (#{id}, #{name }, #{phone }, #{ip }, now(), #{userNo})	
	  	]]>
	  		
	</insert>
</mapper>
